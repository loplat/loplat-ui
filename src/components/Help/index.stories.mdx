import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { Help } from './index.tsx';

<Meta
  title="Components/Help"
  component={Help}
  argTypes={{
    title: { description: '`Help`컴포넌트의 상단 제목입니다. `p`태그를 사용합니다.' },
    text: { description: '`Help`컴포넌트의 본문내용입니다. `p`태그를 사용합니다.' },
    maxWidth: {
      description: '`px`단위로 숫자를 입력하거나, `100%`를 입력하면 됩니다.',
      options: [200, 400, 600, 800, 1000, '100%'],
      control: { type: 'select' },
    },
  }}
/>

export const Template = (args) => (
  <Help
    title="샘플 텍스트입니다."
    text="풀밭에 싸인 청춘을 끝에 듣기만 하여도 만물은 그들은 것이다. 이상은 소금이라 든 풀밭에 있는가?"
    {...args}
  />
);

# Help

<Canvas>
  <Story name="default">{Template.bind({})}</Story>
</Canvas>

## Props

<ArgsTable story="default" />

## 예제

### fullWidth

`width: 100%`속성을 갖습니다.

<Canvas>
  <Story name="100% width" args={{ maxWidth: '100%' }}>
    {Template.bind({})}
  </Story>
</Canvas>
