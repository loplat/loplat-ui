import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { Badge } from './index.tsx';
import { css } from '@emotion/css';
import { spacing } from '../../storybook-props';

<Meta
  title="Components/Badge"
  component={Badge}
  argTypes={{
    ...spacing('margin'),
  }}
/>

export const Template = (args) => <Badge {...args} />;

# Badge

<Canvas>
  <Story name="default" args={{ children: 'default' }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="default" />

## 예제

<Canvas>
  <Story name="primary1" args={{ children: 'primary1', variant: 'primary1' }}>
    {Template.bind({})}
  </Story>
  <Story name="primary2" args={{ children: 'primary2', variant: 'primary2' }}>
    {Template.bind({})}
  </Story>
  <Story name="success1" args={{ children: 'success1', variant: 'success1' }}>
    {Template.bind({})}
  </Story>
  <Story name="success2" args={{ children: 'success2', variant: 'success2' }}>
    {Template.bind({})}
  </Story>
  <Story name="danger1" args={{ children: 'danger1', variant: 'danger1' }}>
    {Template.bind({})}
  </Story>
  <Story name="danger2" args={{ children: 'danger2', variant: 'danger2' }}>
    {Template.bind({})}
  </Story>
  <Story name="light1" args={{ children: 'light1', variant: 'light1' }}>
    {Template.bind({})}
  </Story>
  <Story name="light2" args={{ children: 'light2', variant: 'light2' }}>
    {Template.bind({})}
  </Story>
  <Story name="done1" args={{ children: 'done1', variant: 'done1' }}>
    {Template.bind({})}
  </Story>
  <Story name="done2" args={{ children: 'done2', variant: 'done2' }}>
    {Template.bind({})}
  </Story>
  <Story name="selected" args={{ children: 'selected', variant: 'selected' }}>
    {Template.bind({})}
  </Story>
</Canvas>

## size

<Canvas>
  <Story name="small" args={{ children: 'small', variant: 'primary1', size: 'small' }}>
    {Template.bind({})}
  </Story>
  <Story name="large" args={{ children: 'large', variant: 'primary1', size: 'large' }}>
    {Template.bind({})}
  </Story>
</Canvas>
