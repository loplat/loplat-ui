import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { CircleSpinner } from './index';
import { spinner } from '../../storybook-props';

<Meta
  title="Components/Spinner/Circle"
  component={CircleSpinner}
  argTypes={{
    ...spinner('circle'),
  }}
/>

export const Template = (args) => <CircleSpinner zIndex="9999" {...args} />;

# CircleSpinner

<Canvas>
  <Story name="default">{Template.bind({})}</Story>
</Canvas>

## Props

<ArgsTable story="default" />

## 예제

### 작은 크기

<Canvas>
  <Story name="circle small" args={{ duration: 1000, scale: 0.5 }}>
    {Template.bind({})}
  </Story>
</Canvas>

### 느린 속도

<Canvas>
  <Story name="circle duration" args={{ duration: 1600, scale: 0.5 }}>
    {Template.bind({})}
  </Story>
</Canvas>
