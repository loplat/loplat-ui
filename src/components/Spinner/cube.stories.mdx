import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { CubeSpinner } from './index';
import { spinner } from '../../storybook-props/';

<Meta
  title="Components/Spinner/Cube"
  component={CubeSpinner}
  argTypes={{
    ...spinner('cube'),
  }}
/>

export const Template = (args) => <CubeSpinner {...args} />;

# CubeSpinner

<Canvas>
  <Story name="cube">{Template.bind({})}</Story>
</Canvas>

## Props

<ArgsTable story="cube" />

## 예제

### 작은 크기

<Canvas>
  <Story name="cube small" args={{ duration: 1000, scale: 0.5, zIndex: 9999 }}>
    {Template.bind({})}
  </Story>
</Canvas>

### 빠른 속도

<Canvas>
  <Story name="cube duration" args={{ duration: 300, scale: 0.5, zIndex: 9999 }}>
    {Template.bind({})}
  </Story>
</Canvas>
