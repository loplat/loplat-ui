import React from 'react';

export type IconProps = {
  size?: number;
  fillColor?: string;
  suffixForId?: string;
  className?: string;
  style?: React.CSSProperties;
};

export * from './export.generated';
