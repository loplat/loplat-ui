import { Meta, ColorPalette, ColorItem, Source } from '@storybook/addon-docs';
import { hideCanvas } from '../../../storybook-props/';
import { palette } from '.';

<Meta title="core/palette" />

# Palette

**loplat**에서 사용하는 팔레트입니다.

## 사용 방법

<Source
  language="js"
  dark
  code={`
     import { primary, success } from 'loplat-ui';
  `}
/>

## 색상 목록

<ColorPalette>
  {Object.entries(palette).map(([paletteName, color]) => (
    <ColorItem key={paletteName} title={paletteName} colors={{ [paletteName]: color }} />
  ))}
</ColorPalette>
