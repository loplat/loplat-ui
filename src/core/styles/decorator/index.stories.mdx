import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { generateTable } from '../../../storybook-props';
import { Highlight, Underline } from './index.stories.tsx';

<Meta title="core/decorator" />

# Underline

`SerializedStyles`를 적용하여 밑줄을 긋습니다.

<Canvas>
  <Story
    name="underline"
    argTypes={{
      borderRadius: {
        description: '`underline`의 border-radius 입니다.',
        type: 'string',
        ...generateTable({ defaultValue: '2px' }),
      },
      bottom: {
        description: '`underline`이 요소의 bottom과 떨어져있는 정도입니다.',
        type: 'string',
        ...generateTable({ defaultValue: '-8px' }),
      },
      color: {
        description: '`underline`의 색상입니다.',
        type: 'string',
        ...generateTable({ defaultValue: '#0c8efc' }),
      },
      height: { description: '`underline`의 높이입니다.', type: 'string', ...generateTable({ defaultValue: '3px' }) },
      width: {
        description: '`underline`의 너비입니다.',
        type: 'string',
        ...generateTable({ defaultValue: '10px' }),
      },
    }}
  >
    {(args) => <Underline {...args} />}
  </Story>
</Canvas>

<ArgsTable story="underline" />

## 사용 방법

`underline`은 `@emotion`의 `SerializedStyles`를 return 하는 함수입니다.

적용하기 위해서는 `underline`를 사용하는 styled 컴포넌트를 생성해야 합니다.

```js
import { styled, underline } from 'loplat-ui';

const Underline = styled.span`
  ${underline({ borderRadius, bottom, color, height, width })};
`;
```

# Highlight

`SerializedStyles`를 적용하여 강조 효과를 줍니다.

<Canvas>
  <Story
    name="highlight"
    argTypes={{
      color: {
        description: '`highlight`의 색상입니다.',
        type: 'string',
        ...generateTable({ defaultValue: '#ebf6fe' }),
      },
      positionX: {
        description: '`highlight` 시작점의 X좌표 입니다.',
        type: 'string',
        ...generateTable({ defaultValue: '50%' }),
      },
      width: { description: '`highlight`의 너비입니다.', type: 'string', ...generateTable({ defaultValue: '100%' }) },
    }}
  >
    {(args) => <Highlight {...args} />}
  </Story>
</Canvas>

<ArgsTable story="highlight" />

## 사용 방법

`highlight`는 `@emotion`의 `SerializedStyles`를 return 하는 함수입니다.

적용하기 위해서는 `highlight`를 사용하는 styled 컴포넌트를 생성해야 합니다.

```js
import { styled, highlight } from 'loplat-ui';

const Highlight = styled.span`
  ${highlight({ color, positionX, width })};
`;
```
